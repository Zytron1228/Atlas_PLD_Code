#include "main.h"
/**
 * main - descr.
 *
 * Return: 0
 */
int isPalindrome(char *string)
{
	

	int i = 0;
	int n = 0;
	int size = 0;

	while (s[i] != '\0')
	{
		i++;
	}
	char rev[i];

	i--;
	size = i;
	while (i >= 0)
	{
		char a = string[n];

		string[n] = string[i];
		string[i] = a;
		n++;
		i--;
}


	return (0);
}
